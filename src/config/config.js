const path = require("path");


// TODO:
// 1. Require npm package `dotenv` and and node module `path`
// 2. load .env variables to process.env [TIP: Use dotenv.config() method to pass path for .env files ]
// 3. Destructure environment variable PORT and MONGODB_CONNECTION_URI from process.env
// 4. Export Destructured variables using module.exports